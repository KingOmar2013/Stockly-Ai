{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "node build-standalone.mjs && node scripts/prepare-dist.mjs",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
}
